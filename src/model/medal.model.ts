/**
 * Medal domain model.
 *
 * Medal sprites live in GameSprites.s_medals (keyed by MedalKey).
 * Medal threshold logic lives in medal.controller.ts.
 * Medal type definitions live in types/medal.types.ts.
 *
 * This file is reserved for any future medal behavior (e.g. animations, unlock tracking).
 */
